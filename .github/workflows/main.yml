name: Build, test and publish nuget package
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
env:
  DOTNET_VERSION: '10.0.x'
  SOLUTION_PATH: './src/MartinDrozdik.DDD.slnx'
  MODELS_PROJECT: './src/MartinDrozdik.DDD/MartinDrozdik.DDD.csproj'
  MODELS_PROJECT_TESTS: './src/MartinDrozdik.DDD.Tests/MartinDrozdik.DDD.Tests.csproj'
  DEMO_PROJECT_PATH: './src/MartinDrozdik.DDD.Demo/MartinDrozdik.DDD.Demo.csproj'
  DEMO_PROJECT_CLIENT_PATH: './src/MartinDrozdik.DDD.Demo.Client/MartinDrozdik.DDD.Demo.Client.csproj'
  DEMO_PROJECT_TESTS: './src/MartinDrozdik.DDD.Demo.Tests/MartinDrozdik.DDD.Demo.Tests.csproj'
  WEB_PROJECT: './src/MartinDrozdik.DDD.Web/MartinDrozdik.DDD.Web.csproj'
  WEB_PROJECT_TESTS: './src/MartinDrozdik.DDD.Web.Tests/MartinDrozdik.DDD.Web.Tests.csproj'
jobs:
  validate-markdown:
    name: Validate Markdown Links
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      
    - name: Check markdown links
      uses: tcort/github-action-markdown-link-check@v1
        
  build:
    name: Build
    runs-on: ubuntu-latest
    needs: validate-markdown
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Display .NET version
      run: dotnet --version
        
    - name: Restore dependencies
      run: dotnet restore ${{ env.SOLUTION_PATH }}
      
    - name: Build solution
      run: dotnet build ${{ env.SOLUTION_PATH }} --no-restore --configuration Release
      
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-artifacts
        path: |
          src/**/bin/Release/**
          src/**/obj/Release/**
        retention-days: 1

  test:
    name: Tests – ${{ matrix.project_name }}
    runs-on: ubuntu-latest
    needs: build
    permissions: # for test results
      contents: read
      issues: read
      checks: write
    strategy:
      fail-fast: false # Keep running other tests if one fails
      matrix:
        include:
          - project_name: "Models"
            path: './src/MartinDrozdik.DDD.Tests/MartinDrozdik.DDD.Tests.csproj'
            executable: 'src/MartinDrozdik.DDD.Tests/bin/Release/net10.0/MartinDrozdik.DDD.Tests'
          - project_name: "Web"
            path: './src/MartinDrozdik.DDD.Web.Tests/MartinDrozdik.DDD.Web.Tests.csproj'
            executable: 'src/MartinDrozdik.DDD.Web.Tests/bin/Release/net10.0/MartinDrozdik.DDD.Web.Tests'
          - project_name: "Demo"
            path: './src/MartinDrozdik.DDD.Demo.Tests/MartinDrozdik.DDD.Demo.Tests.csproj'
            executable: 'src/MartinDrozdik.DDD.Demo.Tests/bin/Release/net10.0/MartinDrozdik.DDD.Demo.Tests'

    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
      
    - name: Restore dependencies
      run: dotnet restore ${{ env.SOLUTION_PATH }}
    
    - name: Download build artifacts
      uses: actions/download-artifact@v8
      with:
        name: build-artifacts
        path: src/

    # Required for xUnit runnner
    - name: Make test binary executable
      run: chmod +x ${{ matrix.executable }}
      
    - name: Run model tests
      run: dotnet test ${{ matrix.path }} --no-build --configuration Release --verbosity normal --logger "trx;LogFileName=${{ matrix.project_name }}-results.trx" --results-directory ./TestResults
    
    - name: Publish test results
      uses: EnricoMi/publish-unit-test-result-action@v2
      if: always()
      with:
        comment_mode: off
        files: |
          TestResults/**/*.trx
        check_name: Test results – ${{ matrix.project_name }}

  publish-nuget:
    name: Publish NuGet – ${{ matrix.project_name }}
    runs-on: ubuntu-latest
    needs: [build, test]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    strategy:
      matrix:
        include:
          - project_name: "Models"
            path: './src/MartinDrozdik.DDD/MartinDrozdik.DDD.csproj'
          - project_name: "Web"
            path: './src/MartinDrozdik.DDD.Web/MartinDrozdik.DDD.Web.csproj'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
        
    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
        
    - name: Restore dependencies
      run: dotnet restore ${{ env.SOLUTION_PATH }}
      
    - name: Build NuGet project
      run: dotnet build ${{ matrix.path }} --configuration Release --no-restore
      
    - name: Pack NuGet package
      run: dotnet pack ${{ matrix.path }} --configuration Release --no-build --output ./packages
      
    - name: Publish to NuGet
      run: dotnet nuget push ./packages/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate